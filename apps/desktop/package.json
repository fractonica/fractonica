{
  "name": "@fractonica/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "allow-local-network": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/allow-local-network.ps1",
    "bundle": "node scripts/build.mjs",
    "dev": "pnpm prepare:sidecar && tauri dev",
    "prepare:sidecar": "node scripts/prepare-sidecar.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
